{
  "name": "usdata-site",
  "private": true,
  "type": "module",
  "scripts": {
    "types": "wrangler types",
    "check": "wrangler types && tsc --noEmit && vitest run",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "build": "cd .. && UV_PROJECT_ENVIRONMENT=.venv-docs uv run --locked --group docs --no-default-groups python scripts/docs_site.py build"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.22.0",
    "@types/node": "22.20.2",
    "prettier": "3.9.6",
    "typescript": "7.0.2",
    "vitest": "4.1.11",
    "wrangler": "4.130.0"
  },
  "overrides": {
    "workerd": "1.20260908.1",
    "sharp": "0.35.4"
  }
}
