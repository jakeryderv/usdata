{
  "name": "usdata-infra",
  "private": true,
  "type": "module",
  "scripts": {
    "check:docs": "wrangler deploy --config docs.wrangler.jsonc --dry-run",
    "dev:docs": "wrangler dev --config docs.wrangler.jsonc --port 8788",
    "deploy:docs": "wrangler deploy --config docs.wrangler.jsonc"
  },
  "devDependencies": {
    "wrangler": "4.130.0"
  },
  "overrides": {
    "workerd": "1.20260908.1",
    "sharp": "0.35.4"
  }
}
