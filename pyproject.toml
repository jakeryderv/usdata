[project]
name = "usdata"
version = "0.1.0"
description = "Utilities for working with US public data"
readme = "README.md"
authors = [
    { name = "Jake Van Slyke", email = "jakervanslyke@gmail.com" }
]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
usdata = "usdata:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
