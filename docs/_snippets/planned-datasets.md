!!! note "Planned datasets are not fetchable"
    A planned entry describes a source the registry knows about but has no
    adapter for. Search shows it only with `--include-planned`, `fetch` exits
    3, and the [versions and targets](/generated/catalog/versions.md) page
    lists its status.
