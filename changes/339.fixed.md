A restore now treats a pinned object that is gone (HTTP 404 or 410) or a refused range request as drift, so a configured mirror restores it instead of the pull failing; a mirror that cannot be reached is reported with the drift rather than aborting the run.
