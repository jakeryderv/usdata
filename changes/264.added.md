Fetch observed CO-OPS current speed and direction with `noaa:coops-currents`, selecting one station and explicit bin in UTC. A Cape Henry worked example reports gaps, preserves dated depth metadata, and verifies byte-for-byte restoration into an empty cache.
