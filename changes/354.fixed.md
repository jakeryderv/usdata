`Grib2Summary.variable_for` now names a message ecCodes has no short name for as the GRIB2 reader does, `parameter_<discipline>_<category>_<number>` or the MRMS product, so the name it returns is a variable in the opened Dataset. Each inspected `GribMessage` carries that name as `base_name`.
