An adapter contract test probes every available dataset with a bbox, a variable, and a window before any transport, so a capability the registry declares but the adapter refuses now fails the offline suite; the declared `limits.max_window` checks moved beside it.
