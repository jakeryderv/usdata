Storm Events CSVs opened with the pandas reader gain `BEGIN_UTC` and `END_UTC` columns, derived from the local timestamps and the `CZ_TIMEZONE` offset label and recorded under `frame.attrs["usdata"]["derived"]`.
