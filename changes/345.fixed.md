NetCDF4 and GRIB2 readers no longer stamp registry units on a decoded time variable, whose units the file stated and xarray moved into its encoding, so a GLM Dataset opened with `open_netcdf` writes back with `to_netcdf()`.
