`usdata info` now prints what a dataset delivers and takes: its file formats, the optional extra that opens them (or `none` for formats with no bundled reader), the selection rule, the required inputs, and the worked examples that use it.
