No release note: this completes the adapter parameter migration and removes the transitional hand-written `accepted_params` form, so a declared `params_model` is now the only way an adapter states what it accepts. The only behavior change is that an explicit null for the NEXRAD `site`/`sites` selectors now means "not given" rather than an error, matching how the station adapters already read one.
