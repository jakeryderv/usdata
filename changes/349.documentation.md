The Storm Events and SPC guides now say that their annual files are split by local date while windows select them by UTC year, so a window within a few hours of a new year fetches only one file, and how to widen it to fetch both.
