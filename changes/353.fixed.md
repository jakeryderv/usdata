GRIB2 `units` holding a non-integer power, such as ecCodes' `m**(2/3) s**-1` for eddy dissipation, now keep the file's spelling instead of becoming the invalid `m(2/3) s-1`; only integer powers of a unit symbol take the UDUNITS notation.
