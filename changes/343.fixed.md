A date alone as the end of a window now covers that whole UTC day in every ISO 8601 spelling Python reads, not only `2024-01-15`: the compact `20240115` and the week form `2024-W03-1` used to resolve to midnight and silently drop the last day.
