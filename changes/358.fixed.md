The GRIB2 reader now names an MRMS `LightningProbabilityNext30minGrid_scale_1` field after its product, as it names the other MRMS products, instead of a `parameter_209_...` fallback, so its registry units (`%`) and long name are filled.
