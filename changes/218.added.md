The six archive-backed examples commit their lockfiles, the offline checks hold each to its manifest, the example pages offer the lockfile for download, and a weekly `restore` job pulls every one into an empty cache and reports the assets that drifted.
