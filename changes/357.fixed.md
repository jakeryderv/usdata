Search text with no letters or digits, such as `usdata search '?'`, is now refused as bad input (exit 2, or `ValueError` from `usdata.search`) instead of listing every dataset as a match. Blank or absent text still applies no keyword filter.
