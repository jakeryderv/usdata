Manifest sources take an optional `name`, and a pull result groups what it fetched under `by_source`, keyed by that name or by the source's one-based position; `fetched` stays in manifest order, then adapter listing order.
