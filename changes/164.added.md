`usdata doctor` reports the interpreter, reader extras and the ecCodes library, the cache directory and its free space, the cache environment variables, and, with `--network`, whether the upstream hosts answer; it exits 1 when a check fails.
