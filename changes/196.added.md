`Citation` is exported from the package root and renders itself through `as_text()` and `as_bibtex()`, so citations no longer have to be produced by shelling out to the console script, and `python -m usdata` now runs the CLI.
