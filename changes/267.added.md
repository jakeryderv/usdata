Select GOES ABI mesoscale imagery with `product: ABI-L2-CMIPM` and explicit `sector: M1` or `M2`. A fifteen-minute central Plains example checks scan timing, footprint stability, and quality flags, and restores its committed scene lockfile into an empty cache.
