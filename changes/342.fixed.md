`epa:aqs-daily` now paces transport retries as well as first requests: a retry after a 429, a 5xx, or a timeout waits until six seconds have passed since the previous attempt, or longer when `Retry-After` asks, instead of reaching EPA again within about a second. `usdata.protocols.http.get` gains a `before_attempt` callable for this, run before every attempt.
