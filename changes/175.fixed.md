`usdata pull` takes `--quiet` to print only the summary line instead of one line per asset, and its `--no-progress` flag no longer advertises a `--no-no-progress` counterpart; NEXRAD's `nearest` now rejects non-ASCII digit strings like the other integer parameters do.
