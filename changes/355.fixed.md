`usdata fetch --dry-run` now exits 1 when no asset matched, as `fetch`, `search`, and `datasets` already do, so a script can use a dry run to test whether data exists. It still prints `0 asset(s) matched`, and `--json` still prints `[]`.
