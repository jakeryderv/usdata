NetCDF4 and GRIB2 results now take `units` a file leaves missing or `unknown` and missing `long_name` values from the registry entry's variable table, never overwriting what the file provides; every fill is listed under `attrs["usdata"]["registry_attrs"]`. MRMS fields, which decode without the product's level suffix, gain the units the files omit.
